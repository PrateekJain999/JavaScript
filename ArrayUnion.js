const { some } = require('lodash')
const lodash = require('lodash')
a=[1,2,3,4,5]
b=[6,7,8]

// c=a.concat(b)
// c=[...a,...b]
// c=lodash.concat(a,b)
// c=a.concat(b).sort();
// c=c.filter((value,pos) => {return c.indexOf(value) == pos})
// c=lodash.union(a,b)

// console.log(c)